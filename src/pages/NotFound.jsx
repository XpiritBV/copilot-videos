import React from 'react';

const NotFound = () => {
    console.log('NotFound component rendered');
    return (
        <div>
            Not found
        </div>
    );
};

export default NotFound;